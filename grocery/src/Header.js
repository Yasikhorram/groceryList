const Header = ({ title }) => {
  return (
    <header
      style={{
        backgroundColor: "#D3D2CF",
        color: "#B39558",
      }}
    >
      <h1> {title}</h1>
    </header>
  );
};

export default Header;
